<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">EASI-DESTI</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link " href="#">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.pathInfo == path('admin_series') ? 'active' : '' }}" href="{{ path('admin_series') }}">Liste des séries</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.pathInfo == path('admin_schools') ? 'active' : '' }}" href="{{ path('admin_schools') }}">Statistiques</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.pathInfo == path('word_new') ? 'active' : '' }}" href="{{ path('word_new') }}">Créer un nouveau mot</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link">Créer un compte enseignant</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle {{ app.request.pathInfo == path('admin_registration_select_school') ? 'active' : '' }}" href="#" id="navbarScrollingDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Gestion des élèves
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarScrollingDropdown">
                        <li><a class="dropdown-item" href="{{ path('admin_registration_select_school') }}">Création d'un élève</a></li>
                        <li><a class="dropdown-item" href="{{ path('admin_registration_select_student_for_edit_student') }}">Modification d'un élève</a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle {{ app.request.pathInfo == path('admin_school_show_school') ? 'active' : '' }}" href="#" id="navbarScrollingDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Gestion des établissements
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarScrollingDropdown">
                        <li><a class="dropdown-item" href="{{ path('admin_school_new_school') }}">Création d'une école</a></li>
                        <li><a class="dropdown-item" href="{{ path('admin_school_show_school') }}">Modification d'une école</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('admin_school_level_new') }}">Création d'une classe</a></li>
                        <li><a class="dropdown-item" href="{{ path('admin_school_level_select_school_level') }}">Modification d'une classe</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>
            </ul>
        </div>
    </div>
</nav>