{% extends 'base.html.twig' %}

{% block title %}EASI{% endblock %}

{% block body %}
    <div class="container-fluid easi">
        <div class="row pt-3">
            <div class="col-lg-2">
                <img class="ms-5 img-letter" src="{{ asset('build/images/image_a.png') }}" alt="">
            </div>
            <div class="d-flex justify-content-center">
                <div class="col-6 d-flex flex-column align-items-center">
                    <img src="{{ asset('build/images/emoji_error.png') }}" class="emoji" alt="error">
                    {# fichier son lecture automatique #}
                    <h1>Dommage </h1>
                    <p>Tu as une dernière chance, saisie la ! N'oublie pas les aides ! 1 essais restants</p>
                </div>
            </div>
            <div class="d-flex justify-content-center align-text-bottom">
                <a href="">
                    <div class="next btn btn-app bg-secondary p-2 fs-2">Réessayer<i class="fas fa-arrow-right mx-3"></i></div>
                </a>
            </div>
        </div>
    </div>
{% endblock %}