{% extends 'easi/help_layout.html.twig' %}
{% block help_number %}
    2
{% endblock %}

{% block help_content %}
    {% if word.picture is not null%}
        <p class="btn-app word-help fs-1 my-3">image du mot : {{ word.content }}</p>
        <img src=" {{ asset('images/word/' ~ word.picture) }}" class="img-help border border-primary border-2 rounded-pill my-3" alt="help">
    {% else %}
        <p class="btn-app text-center word fs-1 my-3">{{ word.content }}</p>
        <p class="btn-app word-help text-center fs-1 border border-primary border-2 my-5"><span class="text-decoration-underline">Définition :</span> {{ word.definition }}</p>
    {% endif %}
{% endblock %}

